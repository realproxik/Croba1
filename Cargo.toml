[package]
name = "croba-tokenizer-rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "croba_tokenizer_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

